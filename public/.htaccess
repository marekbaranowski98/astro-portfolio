RewriteEngine On

# HTTPS: użyj regex zamiast "!=on"
RewriteCond %{HTTPS} !^on$ [OR]
RewriteCond %{HTTP_HOST} ^www\.marekbaranowski\.net$ [NC]
RewriteRule ^ https://marekbaranowski.net%{REQUEST_URI} [R=301,L]
